services:
  nime-fetch:
    build: .
    container_name: nime-fetch
    restart: unless-stopped
    volumes:
      - ./downloads:/downloads
      - ./database:/database
    ports:
      - 3000:3000


